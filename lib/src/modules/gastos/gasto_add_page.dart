import 'package:flutter/material.dart';

class GastoAddPage extends StatelessWidget {
  const GastoAddPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Gasto Add Page'),
      ),
      body: Container(),
    );
  }
}
